const e = [
  {
    hash: "c2944a20692a13a8572fb40232cd1151",
    file: "nvidia-marketing-icons.woff2",
    format: "woff2"
  },
  {
    hash: "b228dbb674ace48c35b4b62bb9eda614",
    file: "nvidia-marketing-icons.woff",
    format: "woff"
  }
], f = "2.1.0", g = "assets/marketing-icons-web-font", o = "brand-assets.cne.ngc.nvidia.com", a = (t) => t ? `${t}` : `https://${o}/${g}/${f}`, m = () => `https://${o}`, h = (t) => t.replaceAll("-", "_"), i = "nvidia-marketing-icons", d = (t = a(), n = e, r = i) => [
  "@font-face {",
  '  font-family: "missing-ligature-font";',
  /* find any font that is available on the system, so we can size-adjust to 0 to make invisible */
  "  src: local(Arial), local(Verdana), local(Tahoma), local(Trebuchet MS);",
  "  size-adjust: 0%;",
  "}",
  "@font-face {",
  `  font-family: "${r}";`,
  "  src:",
  `${n.map(
    ({ file: c, format: s, hash: l }) => `    url(${t}/${c}?${l}) format(${s})`
  ).join(`,
`)};`,
  /* ensure we do not show the icon ligature name if the font does not load */
  "  font-display: block;",
  "}"
].join(`
`), b = (t = i) => `[data-nv-marketing-icon-before], [data-nv-marketing-icon-after] {
  font-family: '${t}', 'missing-ligature-font';
  font-weight: normal;
  font-style: normal;
  font-size: 1em;
  display: inline-block;
  vertical-align: top;
  width: 1em;
  height: 1em;
  line-height: 1;
  text-transform: none;
  letter-spacing: normal;
  word-wrap: normal;
  white-space: nowrap;
  overflow: hidden;
  direction: ltr;
  /* Support for all WebKit browsers. */
  -webkit-font-smoothing: antialiased;
   
  /* Support for Safari and Chrome. */
  text-rendering: optimizeLegibility;

  /* Support for Firefox. */
  -moz-osx-font-smoothing: grayscale;
}

[data-nv-marketing-icon-before]::before {
  content: attr(data-nv-marketing-icon-before);
}

[data-nv-marketing-icon-after]::after {
  content: attr(data-nv-marketing-icon-after);
}
`, p = (t, n) => ({
  name: "nvidia-marketing-icons-plugin",
  transformIndexHtml() {
    return [
      // https://web.dev/articles/font-best-practices#preconnect_to_critical_third-party_origins
      {
        tag: "link",
        attrs: {
          rel: "preconnect",
          href: m(),
          crossorigin: !0
        }
      },
      {
        tag: "style",
        children: d(
          a(n),
          e,
          t
        )
      },
      {
        tag: "style",
        children: b(t)
      }
    ];
  }
});
export {
  i as DEFAULT_FONT_FAMILY,
  p as NvidiaMarketingIconsVitePlugin,
  e as fontFiles,
  d as getFontFace,
  a as getFontFileUrlBase,
  b as getFontStyles,
  h as getIconNameForFont,
  m as getPreconnectUrl
};
